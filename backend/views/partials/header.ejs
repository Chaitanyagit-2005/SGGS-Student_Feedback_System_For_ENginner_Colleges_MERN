<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title><%= typeof user !== 'undefined' && user?.role ? user.role.toUpperCase() : 'Panel' %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet"/>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css"/>
  <style>
    .nav-link {
      color: #fff;
    }
    .nav-link:hover {
      background-color: rgba(255, 255, 255, 0.1);
      border-radius: 5px;
    }
  </style>
</head>
<body>
  <div class="container-fluid">
    <div class="row">
      <% if (user) { %>
        <!-- Sidebar Column -->
        <div class="col-md-3 col-lg-2 bg-dark text-white min-vh-100 p-3">
          <h5 class="text-white mb-4">👋 Welcome, <%= user.name %></h5>

          <!-- Sidebar toggle for mobile -->
          <button class="btn btn-sm btn-light mb-3 d-md-none" id="toggleSidebar">
            <i class="fas fa-bars"></i> Toggle Menu
          </button>

          <div id="sidebarMenu">
            <%- include("sidebar") %>
          </div>
        </div>

        <!-- Main Content Column -->
        <div class="col-md-9 col-lg-10 p-4">
      <% } else { %>
        <!-- Full Width for Unauthenticated Users -->
        <div class="col-12 p-4">
      <% } %>
